"""Domain layer: pure business model and port definitions.

No I/O, no framework, no infrastructure. May import only ``vera.shared`` (and
stdlib/pydantic). Enforced by the import-linter contract in ``pyproject.toml``.
"""
